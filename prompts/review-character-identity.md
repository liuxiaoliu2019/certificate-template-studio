# 角色身份独立验收

对候选中实际出现的每个角色分别验收。必须同时查看完整封面、该角色原图裁切、身份档案和当前候选；报告绑定角色 ID、裁切哈希与候选哈希。

发型或头部、脸部、服装或表面分区、标志性配件、物种、身体比例逐项给出布尔结果，并给出 0–100 身份分。任一不可改变项失败或总分低于 85 即 `hard_fail`。姿势、动作、朝向和绘制媒介的改变本身不扣身份分。

来源图片不足以确认特征时记录 `blocked`，只向用户询问更清晰的对应角色图片，不得猜测或用设计美感抵消身份缺失。
